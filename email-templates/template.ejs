<!DOCTYPE html>
<html>
    <head>
        <title><%= site %></title>
    </head>
    <body>
        <table>
            <% for(var i=0; i < 4; i+=2) { %>
                <tr>
                <% for(var j=0; j < 2; j++) { %>
                    <td><%= sites[i + j] %></td>
                 <% } %>
                </tr>
             <% } %>
        </table>

    </body>
</html>