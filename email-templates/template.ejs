<!DOCTYPE html>
<html>
    <head>
        <title><%= site %></title>
    </head>
    <body>
        <% for(var i=0; i < 3; i++) { %>
            <h2><%= site %></h2>
         <% } %>
    </body>
</html>